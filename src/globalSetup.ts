if (typeof (global as any).__USE_AXIOS__ === 'undefined') {
    (global as any).__USE_AXIOS__ = true;
}

if (typeof (global as any).__USE_EVENTSOURCE__ === 'undefined') {
    (global as any).__USE_EVENTSOURCE__ = true;
}
